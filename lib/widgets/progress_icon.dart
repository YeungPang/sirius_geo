import 'package:flutter/cupertino.dart';

class ProgressIcon extends StatelessWidget {
  final Map<String, dynamic> map;

  ProgressIcon(this.map);

  @override
  Widget build(BuildContext context) {
    return Row();
  }
}
